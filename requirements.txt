Django>=5.1.3
python-dotenv>=1.0.1
pytz>=2024.1
sqlparse>=0.4.4
tzdata>=2024.1
gunicorn>=22.0.0       # For production deployment (e.g. on Render or Heroku)
whitenoise>=6.6.0      # For serving static files in production
Pillow>=10.3.0         # For image uploads via ImageField
requests>=2.31.0       # For calling external APIs (e.g., Zoom or Google Meet)
